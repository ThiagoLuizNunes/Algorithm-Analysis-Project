//Algorithms Sort

void insertion(int *vector[]){
	
	int changes = 0;
	int i, x, less;
	int size = sizeof(vecotor)/sizeof(vector[0]);
	
	for(i=0; i<size; i++){
		
	}
	printf("Position: %d", vector[0]);
}
